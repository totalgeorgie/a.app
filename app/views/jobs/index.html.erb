<% provide(:title, 'All Jobs') %>

<div class="row">
	<div class="span12">

<% if current_user && current_user.admin? %>
	<%= render 'shared/dashboard_header'  %>
<% end %>

<%= will_paginate %>

<ul class="jobs">
  <%= render @jobs %>
</ul>

<%= will_paginate %>

</div>
</div>