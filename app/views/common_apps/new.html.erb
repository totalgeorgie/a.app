<% provide(:title, "Sign up!") %>
<%= form_for @common_app, :html => { :multipart => true } do |f| %>
 <%= render 'fields', f: f %>
  <%= f.submit "Submit the application", class: "button" %>
<% end %>
