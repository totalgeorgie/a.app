  <%= render 'shared/error_messages', object: f.object %>
  
  <div class="field">
	<%= f.label :cover_letter %>
	<%= f.text_area :cover_letter %>

	<%= f.label :current_city %>
	<%= f.text_field :current_city %>

	<%= f.label :grad_year %>
	<%= f.text_field :grad_year %>

	<%= f.label :read_type %>
	<%= f.text_field :read_type %>

	<%= f.label :listen_speak %>
	<%= f.text_field :listen_speak %>

	<%= f.label :time_in_china %>
	<%= f.text_field :time_in_china %>

	<%= f.label :resume %>
	<%= f.file_field :resume %>

	<%= f.label "Choose which industries you like" %>

	<%= f.select :industry_ids, Industry.all.collect {|industry| [industry.name, industry.id]}, {}, :multiple => true, :class => "chosen-select" %>

	<%= f.label "Choose which cities you like" %>

	<%= f.select :city_ids, City.all.collect {|city| [city.name, city.id]}, {}, :multiple => true, :class => "chosen-select" %>

	<%= f.label "Choose which roles you like" %>

	<%= f.select :position_ids, Position.all.collect {|city| [city.name, city.id]}, {}, :multiple => true, :class => "chosen-select" %>


  </div>