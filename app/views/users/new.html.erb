<% provide(:title, "Sign up!") %>
    <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages' %>
      <%= render 'fields', f: f %>

      <%= f.submit "Create my account", class: "button" %>
    <% end %>